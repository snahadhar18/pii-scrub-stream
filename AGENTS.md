# Agent Guidelines for pii-scrub-stream

## Author & contributor attribution (MANDATORY)

These rules apply to every authorship/contributor field, including
`pyproject.toml` (`authors`/`maintainers`), `AUTHORS`, `LICENSE`, and the
README "Authors" section, as well as git commit messages and the repository's
GitHub collaborators.

### NEVER remove these contributors

- `snahadhar18`
- `Prakhar SHUKLA <pss317@uowmail.edu.au>`

If either is missing from any attribution field, add them back.

### ALWAYS remove "cursor agent" attribution

- Never list "Cursor", "cursor agent", "Cursor Agent", or any AI/agent tool as
  an author, contributor, maintainer, or co-author.
- Never add `Co-authored-by:` trailers naming Cursor or any AI agent to commits.
  Commits must be authored and committed by the user's own account.
- If such an entry already exists in any file or commit message, remove it.
